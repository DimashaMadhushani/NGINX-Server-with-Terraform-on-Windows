# NGINX-Server-with-Terraform-on-Windows
This tutorial shows how to use Terraform on Windows to deploy an NGINX server in a Docker container. You'll create a config file, initialize Terraform, apply the setup, view the server at localhost:8000, and destroy it when done. It's a simple intro to Infrastructure as Code (IaC) using Docker and Terraform.
